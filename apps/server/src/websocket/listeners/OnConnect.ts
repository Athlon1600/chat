import {SocketSession} from "../SocketSession";

export const onConnect = async function (connection: SocketSession) {
    // TODO: immediately emit info about server: Currently serving 104 connections and 72 users.
}