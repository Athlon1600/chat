import {BaseModel} from "../framework/BaseModel";

// Active Record vs Data Mapper patterns
// Active record is Anti pattern beware of that. It creates a very tight coupling between code and database.
export abstract class EntityModel extends BaseModel {

}